# DataSource settings: set here configurations for the database connection
spring.datasource.url=jdbc:mysql://localhost:3306/enviro_guard?autoReconnect=true
spring.datasource.username=root
spring.datasource.password=cc/123

# Specify the JDBC driver for MySQL
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate settings
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update

# Logging SQL statements (optional, but useful for debugging)
spring.jpa.show-sql=true

